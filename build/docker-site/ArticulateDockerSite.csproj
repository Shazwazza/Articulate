<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFrameworks>net10.0</TargetFrameworks>
    <Nullable>enable</Nullable>
    <AssemblyName>ArticulateDockerSite</AssemblyName>
    <!-- Allow pre-release (rc) package resolution when stable 6.0.0 isn't available locally -->
    <ArticulatePackageVersion Condition="'$(ArticulatePackageVersion)' == ''">6.0.0-*</ArticulatePackageVersion>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Umbraco.Cms" Version="$(UmbracoCmsPackageVersion)" />
    <PackageReference Include="Articulate" Version="$(ArticulatePackageVersion)" />
    <PackageReference Include="Articulate.Theme.Sample" Version="$(ArticulatePackageVersion)" />
  </ItemGroup>
</Project>
