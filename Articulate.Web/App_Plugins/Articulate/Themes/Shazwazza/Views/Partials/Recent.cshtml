@using Articulate
@inherits UmbracoViewPage<Articulate.Models.IMasterModel>

@{
    var recent = Umbraco.GetRecentPosts(Model, 5);
}

<div class="widget recentposts">
    <h4>Recent posts</h4>

    <div class="content">
        <ul class="recentPosts" id="recentPosts">
            @foreach (var post in recent)
            {
                <li>
                    <a href="@post.Url">@post.Name</a>
                    <div class="descr">@post.PublishedDate.ToString("MMMM d, yyyy HH:mm")</div>
                    <div>
                        <a data-disqus-identifier="@post.Id" class="comment-count" href="@post.Url#disqus_thread"></a>
                    </div>
                </li>
            }
        </ul>
    </div>
</div>